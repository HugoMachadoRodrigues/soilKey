# Internal helper: .subtest_result

Internal helper: .subtest_result

## Usage

``` r
.subtest_result(
  passed,
  layers = integer(0),
  missing = character(0),
  details = NULL,
  notes = NA_character_
)
```
