# Compose the v0.1 classification name with disambiguation hints

When the catch-all (Regosols) is assigned but an implemented diagnostic
still passed, the name reflects that the true RSG is identifiable in
principle but not yet wired to the key.

## Usage

``` r
compute_v01_classification_name(rsg, diags, is_default)
```
