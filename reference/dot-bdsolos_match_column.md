# Guess the soilKey horizon column for a BDsolos column name

Returns the canonical soilKey column name, or `NA_character_` if no
pattern matches.

## Usage

``` r
.bdsolos_match_column(raw_name)
```
