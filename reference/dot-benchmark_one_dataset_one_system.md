# Single (dataset, system) benchmark call dispatched by name

Single (dataset, system) benchmark call dispatched by name

## Usage

``` r
.benchmark_one_dataset_one_system(
  ds,
  sys,
  paths,
  max_n,
  harmonize = FALSE,
  verbose = TRUE
)
```
