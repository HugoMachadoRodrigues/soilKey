# Garbic qualifier (ga): \>= 20% organic-waste artefacts (landfill refuse) in the upper 100 cm. v0.9.1 proxy: designation pattern (`Cgarb|garb|landfill|refuse`). Hard schema column `artefacts_garbic_pct` scheduled for v0.9.2.

Garbic qualifier (ga): \>= 20% organic-waste artefacts (landfill refuse)
in the upper 100 cm. v0.9.1 proxy: designation pattern
(`Cgarb|garb|landfill|refuse`). Hard schema column
`artefacts_garbic_pct` scheduled for v0.9.2.

## Usage

``` r
qual_garbic(pedon)
```

## Arguments

- pedon:

  A
  [`PedonRecord`](https://hugomachadorodrigues.github.io/soilKey/reference/PedonRecord.md).
