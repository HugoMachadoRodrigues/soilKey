# Names registered under a USDA level YAML directory (great-groups / suborders).

Names registered under a USDA level YAML directory (great-groups /
suborders).

## Usage

``` r
.coverage_registered_usda_level(subdir, yaml_key)
```
