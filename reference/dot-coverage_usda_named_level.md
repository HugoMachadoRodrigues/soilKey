# USDA coverage at the great-group / suborder level (by NAME vs KST 13ed).

USDA coverage at the great-group / suborder level (by NAME vs KST 13ed).

## Usage

``` r
.coverage_usda_named_level(code_len, subdir, yaml_key, level)
```
