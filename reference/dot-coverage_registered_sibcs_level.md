# Distinct leaf names registered at a SiBCS level (from the merged rule base).

Distinct leaf names registered at a SiBCS level (from the merged rule
base).

## Usage

``` r
.coverage_registered_sibcs_level(yaml_key)
```
