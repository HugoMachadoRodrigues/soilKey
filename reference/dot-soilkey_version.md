# Look up the package version, falling back gracefully when soilKey is not installed (e.g. during interactive development with \`sys.source()\`).

Look up the package version, falling back gracefully when soilKey is not
installed (e.g. during interactive development with \`sys.source()\`).

## Usage

``` r
.soilkey_version()
```
