# SiBCS 5 registered class counts. There is no external canonical code-set to diff against (unlike KST 13ed for USDA), so this honestly reports the registered class counts per level with that caveat – not a percentage.

SiBCS 5 registered class counts. There is no external canonical code-set
to diff against (unlike KST 13ed for USDA), so this honestly reports the
registered class counts per level with that caveat – not a percentage.

## Usage

``` r
.coverage_sibcs()
```
