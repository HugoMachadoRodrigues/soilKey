# Site field that carries a system's reference label (mirrors the \`ref_field\` switch in \`benchmark_run_classification\`).

Site field that carries a system's reference label (mirrors the
\`ref_field\` switch in \`benchmark_run_classification\`).

## Usage

``` r
.benchmark_ref_field(sys)
```
