% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark-loaders.R
\name{benchmark_run_classification}
\alias{benchmark_run_classification}
\title{Run a benchmark across one of the loaded pedon lists}
\usage{
benchmark_run_classification(
  pedons,
  system = c("wrb2022", "sibcs", "usda"),
  level = c("order", "subgroup"),
  boot_n = 1000L
)
}
\arguments{
\item{pedons}{List of \code{\link{PedonRecord}} objects (output of
one of the \code{load_*} functions).}

\item{system}{One of \code{"wrb2022"}, \code{"sibcs"}, \code{"usda"}.}

\item{level}{Granularity of the comparison: \code{"order"} for the
top-level RSG / Ordem / Order; \code{"subgroup"} for the
full assigned name. Default \code{"order"}.}

\item{boot_n}{Bootstrap replicates for CI (default 1000).}
}
\value{
A list with elements \code{accuracy_top1},
        \code{accuracy_ci}, \code{confusion}, and
        \code{per_pedon} (one row per pedon with predicted vs
        reference).
}
\description{
Classifies each pedon in \code{pedons} against the named system,
compares against the published reference (e.g.
\code{site$reference_wrb}), and returns a confusion matrix +
top-1 / top-3 accuracy + bootstrap CI on top-1.
}
